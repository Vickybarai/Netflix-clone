pipeline {
    agent any

    parameters {
        string(name: 'branch', defaultValue: 'main', description: 'Git branch')
    }

    environment {
        DOCKER_IMAGE = 'netflix1'
        DOCKER_REPO = 'baraivicky/netflix-project'
        DOCKER_TAG = 'latest'
        TARGET_IP = '3.238.79.115' 
        TARGET_USER = 'ubuntu'
        CONTAINER_NAME = 'netflix-app'
    }

    stages {
        stage('Git Checkout') {
            steps {
                git branch: "${params.branch}", 
                    credentialsId: 'github-creds', 
                    url: 'https://github.com/Vickybarai/Netflix-clone.git'
                echo '✅ STAGE COMPLETE: Git Checkout finished successfully.'
            }
        }

        stage('Build Docker Image') {
            steps {
                sh "docker build -t ${DOCKER_IMAGE} ."
                echo '✅ STAGE COMPLETE: Docker Image built successfully.'
            }
        }

        stage('Push to Docker Hub') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'dockerhub', usernameVariable: 'DOCKER_USER', passwordVariable: 'DOCKER_PASS')]) {
                    sh """
                        echo "Logging in to Docker Hub..."
                        echo ${DOCKER_PASS} | docker login -u ${DOCKER_USER} --password-stdin
                        echo "Tagging image..."
                        docker tag ${DOCKER_IMAGE} ${DOCKER_REPO}:${DOCKER_TAG}
                        echo "Pushing image..."
                        docker push ${DOCKER_REPO}:${DOCKER_TAG}
                    """
                }
                echo '✅ STAGE COMPLETE: Docker Image pushed to Docker Hub successfully.'
            }
        }

        stage('Deploy to Target Server via SSH') {
            steps {
                sshagent(['ssh-target-creds']) {
                    sh """
                        ssh -o StrictHostKeyChecking=no ${TARGET_USER}@${TARGET_IP} << EOF
                        echo 'Step 1: Stopping specific container named netflix-app...'
                        docker stop ${CONTAINER_NAME} || true
                        docker rm ${CONTAINER_NAME} || true

                        echo 'Step 2: Killing ANY process or container on port 8080 (Force cleanup)...'
                        # Stop any docker container exposing 8080
                        docker ps -q -f "publish=8080" | xargs -r docker stop -t 0
                        # Remove any docker container (even stopped ones) exposing 8080
                        docker ps -a -q -f "publish=8080" | xargs -r docker rm -f
                        
                        echo 'Step 3: Pulling new image...'
                        docker pull ${DOCKER_REPO}:${DOCKER_TAG}
                        
                        echo 'Step 4: Starting new container...'
                        docker run -d --name ${CONTAINER_NAME} -p 8080:8080 ${DOCKER_REPO}:${DOCKER_TAG}
                        EOF
                    """
                }
                echo '✅ STAGE COMPLETE: Successfully deployed to the target server!'
            }
        }
    }

    post {
        always {
            cleanWs()
            echo '🧹 POST ACTION: Workspace cleaned up.'
        }
    }
}